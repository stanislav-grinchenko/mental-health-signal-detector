# Dépendances API slim — sans torch/transformers/ML (déploiement sans modèle)
# Utilisé par Dockerfile.api.slim pour Render / Railway / Fly.io

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1

# Utils
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2

# Langue
langdetect==1.0.9
deep-translator==1.11.4
